/**
 * Handles anti-meridian crossing in geographical data.
 * We need to make sure that when an item and item crosses the anti-meridian (180 degrees longitude),
 * it is handled correctly for mapping and distance calculations.
 * @module AntiMerdianHandling
 *
 */
